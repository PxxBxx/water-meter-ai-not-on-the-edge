# ---- Setup wizard --------------------------------------------------------
flask>=3.0.0

# ---- TFLite runtime – choose ONE of the following blocks.
#
# Option A (recommended): lightweight standalone runtime (~2 MB), no GPU
#   Works on Python 3.9-3.12, x86-64 and arm64.
tflite-runtime>=2.13.0
#
# Option B (alternative): newer Google edge runtime
# ai-edge-litert>=1.0.1
#
# Option C (fallback): full TensorFlow CPU build (larger ~400 MB, Python version restrictions)
# tensorflow-cpu>=2.13.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# Alignment (strongly recommended – without it only coarse rotation is applied)
opencv-python-headless>=4.8.0
